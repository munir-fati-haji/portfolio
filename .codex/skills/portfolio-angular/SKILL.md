## Validation

* Do not run `npm install`, `npm run build`, `npm test`, `npm start`, `npm run watch`, or any other install, validation, watch, or dev-server command unless I explicitly ask you to run it.
* To save tokens and execution time, implement the requested changes and then tell me which command I should run locally to verify them.
* When code changes affect TypeScript, templates, routing, or styling, mention that I should run `npm run build`.
* When behavior or tests are affected, mention that I should run `npm test`.
* When visual inspection is useful, mention that I should run `npm start` and open `http://localhost:4200/`.
* Do not start the dev server automatically. Ask me to run it locally instead.
* If you cannot validate by running commands, clearly say that validation was not executed and list the recommended local checks.
* Do not run install, build, test, watch, or dev-server commands unless I explicitly request it. Prefer telling me the exact command to run locally so I can save tokens.
